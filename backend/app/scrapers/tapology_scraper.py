"""
Tapology scraper placeholder.
Tapology uses Cloudflare protection so BeautifulSoup cannot access it directly.
Use firecrawl_utils.scrape_url(tap_url) to retrieve the page content as markdown,
then pass the markdown text to Claude for field extraction.
"""
